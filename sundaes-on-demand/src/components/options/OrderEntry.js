import Options from "./options";

const OrderEntry = (props) => {
  return (
    <>
      <Options option="scoops"></Options>
      <Options option="toppings"></Options>
    </>
  );
};

export default OrderEntry;
